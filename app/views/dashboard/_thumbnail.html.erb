<div class="btn-group-vertical d-flex px-2" role="group" aria-label="Vertical button group">
  <% @works.each do |work| %>
    <a href="<%= url_for work %>" data-turbo="false" type="button" class="btn btn-info w-100 text-start"> <%= work.name %> </a>
  <% end %>
  <a href="/summary" data-turbo="false" type="button" class="btn btn-primary w-100 bi bi-three-dots opacity-50" title="View all"></a>
</div>
